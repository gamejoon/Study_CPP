1. const int num = 10; : num을 상수화한다.
2. const int * ptr1 = &val1; : 포인터 ptr1을 상수화한다. // 틀림
3. int * const ptr2 = &val2; : ptr2를 통해서 val2의 값을 수정할 수 없다. // 틀림, 2번이랑 바뀜
4. const int * const ptr3 = &val3; : 포인터 ptr3를 상수화하고, ptr3를 통해서 val3의 값을 수정할 수 없다.