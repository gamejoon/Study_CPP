함수를 호출할 때 인자값을 전달하지 않을 경우, 컴파일러가 int형 매개변수를 받는 SimpleFunc함수인지, 매개변수를 받지 않는 SimpleFunc함수인지 구분할 수 없다.


답안지 :
함수 오버로딩의 조건을 만족해서 컴파일은 된다.
그러나 다음과 같이 인자를 전달하지 않으면서 함수를 호출하는 경우, 두 함수 모두 호출조건을 만족하기 때문에 컴파일 에러가 발생한다.
SimpleFunc();

